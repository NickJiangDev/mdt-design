export enum FilterTypeEnum {
  tiled = 'tiled',
  standard = 'standard',
}

/**
 * 字段类型
 */
export enum FieldType {
  text = 'text',
  number = 'number',
  lnglat = 'lnglat',
  datetime = 'datetime',
  bool = 'bool',
  formula = 'formula',
  geometry = 'geometry',
  image = 'image',
}
