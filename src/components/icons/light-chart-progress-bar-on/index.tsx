import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-progress-bar-on.svg';
import createIcon from '../create-icon';

const LightChartProgressBarOn = createIcon(Icon);
export default React.memo(LightChartProgressBarOn);
