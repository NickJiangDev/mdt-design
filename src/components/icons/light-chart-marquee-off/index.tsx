import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-marquee-off.svg';
import createIcon from '../create-icon';

const LightChartMarqueeOff = createIcon(Icon);
export default React.memo(LightChartMarqueeOff);
