import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-map-off.svg';
import createIcon from '../create-icon';

const LightChartMapOff = createIcon(Icon);
export default React.memo(LightChartMapOff);
