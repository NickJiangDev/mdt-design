import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-tree-map-on.svg';
import createIcon from '../create-icon';

const LightChartTreeMapOn = createIcon(Icon);
export default React.memo(LightChartTreeMapOn);
