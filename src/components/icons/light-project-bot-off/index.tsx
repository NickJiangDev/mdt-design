import * as React from 'react';
import { ReactComponent as Icon } from './light-project-bot-off.svg';
import createIcon from '../create-icon';

const LightProjectBotOff = createIcon(Icon);
export default React.memo(LightProjectBotOff);
