import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-list-table-on.svg';
import createIcon from '../create-icon';

const LightChartListTableOn = createIcon(Icon);
export default React.memo(LightChartListTableOn);
