import * as React from 'react';
import { ReactComponent as Icon } from './new-upload-model.svg';
import createIcon from '../create-icon';

const NewUploadModel = createIcon(Icon);
export default React.memo(NewUploadModel);
