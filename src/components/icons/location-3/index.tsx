import * as React from 'react';
import { ReactComponent as Icon } from './location-3.svg';
import createIcon from '../create-icon';

const Location3 = createIcon(Icon);
export default React.memo(Location3);
