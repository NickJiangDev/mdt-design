import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-progress-bar-off.svg';
import createIcon from '../create-icon';

const LightChartProgressBarOff = createIcon(Icon);
export default React.memo(LightChartProgressBarOff);
