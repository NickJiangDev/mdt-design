import * as React from 'react';
import { ReactComponent as Icon } from './join-inner-on.svg';
import createIcon from '../create-icon';

const JoinInnerOn = createIcon(Icon);
export default React.memo(JoinInnerOn);
