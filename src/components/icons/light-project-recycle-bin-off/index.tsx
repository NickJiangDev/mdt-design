import * as React from 'react';
import { ReactComponent as Icon } from './light-project-recycle-bin-off.svg';
import createIcon from '../create-icon';

const LightProjectRecycleBinOff = createIcon(Icon);
export default React.memo(LightProjectRecycleBinOff);
