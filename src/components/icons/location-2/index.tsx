import * as React from 'react';
import { ReactComponent as Icon } from './location-2.svg';
import createIcon from '../create-icon';

const Location2 = createIcon(Icon);
export default React.memo(Location2);
