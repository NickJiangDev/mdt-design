import * as React from 'react';
import { ReactComponent as Icon } from './format-align-left-2.svg';
import createIcon from '../create-icon';

const FormatAlignLeft2 = createIcon(Icon);
export default React.memo(FormatAlignLeft2);
