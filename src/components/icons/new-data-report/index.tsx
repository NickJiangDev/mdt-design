import * as React from 'react';
import { ReactComponent as Icon } from './new-data-report.svg';
import createIcon from '../create-icon';

const NewDataReport = createIcon(Icon);
export default React.memo(NewDataReport);
