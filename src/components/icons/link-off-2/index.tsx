import * as React from 'react';
import { ReactComponent as Icon } from './link-off-2.svg';
import createIcon from '../create-icon';

const LinkOff2 = createIcon(Icon);
export default React.memo(LinkOff2);
