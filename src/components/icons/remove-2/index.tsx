import * as React from 'react';
import { ReactComponent as Icon } from './remove-2.svg';
import createIcon from '../create-icon';

const Remove2 = createIcon(Icon);
export default React.memo(Remove2);
