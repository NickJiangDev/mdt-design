import * as React from 'react';
import { ReactComponent as Icon } from './light-project-map-on.svg';
import createIcon from '../create-icon';

const LightProjectMapOn = createIcon(Icon);
export default React.memo(LightProjectMapOn);
