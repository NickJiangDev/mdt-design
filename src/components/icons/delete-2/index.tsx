import * as React from 'react';
import { ReactComponent as Icon } from './delete-2.svg';
import createIcon from '../create-icon';

const Delete2 = createIcon(Icon);
export default React.memo(Delete2);
