import * as React from 'react';
import { ReactComponent as Icon } from './join-left-on.svg';
import createIcon from '../create-icon';

const JoinLeftOn = createIcon(Icon);
export default React.memo(JoinLeftOn);
