import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-contrast-bar-on.svg';
import createIcon from '../create-icon';

const LightChartContrastBarOn = createIcon(Icon);
export default React.memo(LightChartContrastBarOn);
