import * as React from 'react';
import { ReactComponent as Icon } from './star-off-2.svg';
import createIcon from '../create-icon';

const StarOff2 = createIcon(Icon);
export default React.memo(StarOff2);
