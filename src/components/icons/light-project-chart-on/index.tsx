import * as React from 'react';
import { ReactComponent as Icon } from './light-project-chart-on.svg';
import createIcon from '../create-icon';

const LightProjectChartOn = createIcon(Icon);
export default React.memo(LightProjectChartOn);
