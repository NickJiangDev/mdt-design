import * as React from 'react';
import { ReactComponent as Icon } from './light-project-recycle-bin-on.svg';
import createIcon from '../create-icon';

const LightProjectRecycleBinOn = createIcon(Icon);
export default React.memo(LightProjectRecycleBinOn);
