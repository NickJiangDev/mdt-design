import * as React from 'react';
import { ReactComponent as Icon } from './file-location-to-location-light.svg';
import createIcon from '../create-icon';

const FileLocationToLocationLight = createIcon(Icon);
export default React.memo(FileLocationToLocationLight);
