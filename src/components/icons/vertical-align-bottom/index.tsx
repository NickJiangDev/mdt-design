import * as React from 'react';
import { ReactComponent as Icon } from './vertical-align-bottom.svg';
import createIcon from '../create-icon';

const VerticalAlignBottom = createIcon(Icon);
export default React.memo(VerticalAlignBottom);
