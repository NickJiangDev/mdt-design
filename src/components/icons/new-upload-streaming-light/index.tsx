import * as React from 'react';
import { ReactComponent as Icon } from './new-upload-streaming-light.svg';
import createIcon from '../create-icon';

const NewUploadStreamingLight = createIcon(Icon);
export default React.memo(NewUploadStreamingLight);
