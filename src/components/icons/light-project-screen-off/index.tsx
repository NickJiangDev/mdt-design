import * as React from 'react';
import { ReactComponent as Icon } from './light-project-screen-off.svg';
import createIcon from '../create-icon';

const LightProjectScreenOff = createIcon(Icon);
export default React.memo(LightProjectScreenOff);
