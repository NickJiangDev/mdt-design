import * as React from 'react';
import { ReactComponent as Icon } from './align-ver-center.svg';
import createIcon from '../create-icon';

const AlignVerCenter = createIcon(Icon);
export default React.memo(AlignVerCenter);
