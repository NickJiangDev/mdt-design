import * as React from 'react';
import { ReactComponent as Icon } from './light-project-questionnaire-on.svg';
import createIcon from '../create-icon';

const LightProjectQuestionnaireOn = createIcon(Icon);
export default React.memo(LightProjectQuestionnaireOn);
