import * as React from 'react';
import { ReactComponent as Icon } from './light-project-echarts-on.svg';
import createIcon from '../create-icon';

const LightProjectEchartsOn = createIcon(Icon);
export default React.memo(LightProjectEchartsOn);
