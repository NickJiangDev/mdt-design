import * as React from 'react';
import { ReactComponent as Icon } from './light-project-lab-on.svg';
import createIcon from '../create-icon';

const LightProjectLabOn = createIcon(Icon);
export default React.memo(LightProjectLabOn);
