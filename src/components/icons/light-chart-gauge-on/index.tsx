import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-gauge-on.svg';
import createIcon from '../create-icon';

const LightChartGaugeOn = createIcon(Icon);
export default React.memo(LightChartGaugeOn);
