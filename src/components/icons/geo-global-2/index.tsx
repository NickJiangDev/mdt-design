import * as React from 'react';
import { ReactComponent as Icon } from './geo-global-2.svg';
import createIcon from '../create-icon';

const GeoGlobal2 = createIcon(Icon);
export default React.memo(GeoGlobal2);
