import * as React from 'react';
import { ReactComponent as Icon } from './list-view-report.svg';
import createIcon from '../create-icon';

const ListViewReport = createIcon(Icon);
export default React.memo(ListViewReport);
