import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-scatter-diagram-on.svg';
import createIcon from '../create-icon';

const LightChartScatterDiagramOn = createIcon(Icon);
export default React.memo(LightChartScatterDiagramOn);
