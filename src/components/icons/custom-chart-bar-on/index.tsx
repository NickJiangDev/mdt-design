import * as React from 'react';
import { ReactComponent as Icon } from './custom-chart-bar-on.svg';
import createIcon from '../create-icon';

const CustomChartBarOn = createIcon(Icon);
export default React.memo(CustomChartBarOn);
