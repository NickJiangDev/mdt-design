import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-radar-on.svg';
import createIcon from '../create-icon';

const LightChartRadarOn = createIcon(Icon);
export default React.memo(LightChartRadarOn);
