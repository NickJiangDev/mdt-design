import * as React from 'react';
import { ReactComponent as Icon } from './format-quote-outlined.svg';
import createIcon from '../create-icon';

const FormatQuoteOutlined = createIcon(Icon);
export default React.memo(FormatQuoteOutlined);
