import * as React from 'react';
import { ReactComponent as Icon } from './light-project-bot-on.svg';
import createIcon from '../create-icon';

const LightProjectBotOn = createIcon(Icon);
export default React.memo(LightProjectBotOn);
