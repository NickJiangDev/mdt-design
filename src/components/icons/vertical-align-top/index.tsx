import * as React from 'react';
import { ReactComponent as Icon } from './vertical-align-top.svg';
import createIcon from '../create-icon';

const VerticalAlignTop = createIcon(Icon);
export default React.memo(VerticalAlignTop);
