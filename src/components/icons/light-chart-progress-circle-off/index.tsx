import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-progress-circle-off.svg';
import createIcon from '../create-icon';

const LightChartProgressCircleOff = createIcon(Icon);
export default React.memo(LightChartProgressCircleOff);
