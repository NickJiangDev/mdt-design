import * as React from 'react';
import { ReactComponent as Icon } from './distribute-hor-spacing.svg';
import createIcon from '../create-icon';

const DistributeHorSpacing = createIcon(Icon);
export default React.memo(DistributeHorSpacing);
