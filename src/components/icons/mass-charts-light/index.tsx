import * as React from 'react';
import { ReactComponent as Icon } from './mass-chart-light.svg';
import createIcon from '../create-icon';

const MassChartsLight = createIcon(Icon);
export default React.memo(MassChartsLight);
