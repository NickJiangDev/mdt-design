import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-line-on.svg';
import createIcon from '../create-icon';

const LightChartLineOn = createIcon(Icon);
export default React.memo(LightChartLineOn);
