import * as React from 'react';
import { ReactComponent as Icon } from './join-inner-off.svg';
import createIcon from '../create-icon';

const JoinInnerOff = createIcon(Icon);
export default React.memo(JoinInnerOff);
