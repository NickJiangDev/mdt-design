import * as React from 'react';
import { ReactComponent as Icon } from './light-project-mobile-on.svg';
import createIcon from '../create-icon';

const LightProjectMobileOn = createIcon(Icon);
export default React.memo(LightProjectMobileOn);
