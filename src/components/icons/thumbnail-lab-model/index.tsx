import * as React from 'react';
import { ReactComponent as Icon } from './thumbnail-lab-model.svg';
import createIcon from '../create-icon';

const ThumbnailLabModel = createIcon(Icon);
export default React.memo(ThumbnailLabModel);
