import * as React from 'react';
import { ReactComponent as Icon } from './light-project-resource-off.svg';
import createIcon from '../create-icon';

const LightProjectResourceOff = createIcon(Icon);
export default React.memo(LightProjectResourceOff);
