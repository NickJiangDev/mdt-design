import * as React from 'react';
import { ReactComponent as Icon } from './custom-chart-line-on.svg';
import createIcon from '../create-icon';

const CustomChartLineOn = createIcon(Icon);
export default React.memo(CustomChartLineOn);
