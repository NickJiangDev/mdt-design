import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-marquee-on.svg';
import createIcon from '../create-icon';

const LightChartMarqueeOn = createIcon(Icon);
export default React.memo(LightChartMarqueeOn);
