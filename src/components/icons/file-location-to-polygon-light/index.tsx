import * as React from 'react';
import { ReactComponent as Icon } from './file-location-to-polygon-light.svg';
import createIcon from '../create-icon';

const FileLocationToPolygonLight = createIcon(Icon);
export default React.memo(FileLocationToPolygonLight);
