import * as React from 'react';
import { ReactComponent as Icon } from './radio-off.svg';
import createIcon from '../create-icon';

const RadioOff = createIcon(Icon);
export default React.memo(RadioOff);
