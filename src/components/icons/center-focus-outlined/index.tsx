import * as React from 'react';
import { ReactComponent as Icon } from './center-focus-outlined.svg';
import createIcon from '../create-icon';

const CenterFocusOutlined = createIcon(Icon);
export default React.memo(CenterFocusOutlined);
