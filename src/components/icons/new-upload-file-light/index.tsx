import * as React from 'react';
import { ReactComponent as Icon } from './new-upload-file-light.svg';
import createIcon from '../create-icon';

const NewUploadFileLight = createIcon(Icon);
export default React.memo(NewUploadFileLight);
