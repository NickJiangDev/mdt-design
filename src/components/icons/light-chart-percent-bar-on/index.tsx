import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-percent-bar-on.svg';
import createIcon from '../create-icon';

const LightChartPercentBarOn = createIcon(Icon);
export default React.memo(LightChartPercentBarOn);
