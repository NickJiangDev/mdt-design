import * as React from 'react';
import { ReactComponent as Icon } from './join-left-off.svg';
import createIcon from '../create-icon';

const JoinLeftOff = createIcon(Icon);
export default React.memo(JoinLeftOff);
