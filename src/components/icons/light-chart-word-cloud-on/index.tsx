import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-word-cloud-on.svg';
import createIcon from '../create-icon';

const LightChartWordCloudOn = createIcon(Icon);
export default React.memo(LightChartWordCloudOn);
