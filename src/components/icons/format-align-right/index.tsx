import * as React from 'react';
import { ReactComponent as Icon } from './format-align-right.svg';
import createIcon from '../create-icon';

const FormatAlignRight = createIcon(Icon);
export default React.memo(FormatAlignRight);
