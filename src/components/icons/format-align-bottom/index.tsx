import * as React from 'react';
import { ReactComponent as Icon } from './format-align-bottom.svg';
import createIcon from '../create-icon';

const FormatAlignBottom = createIcon(Icon);
export default React.memo(FormatAlignBottom);
