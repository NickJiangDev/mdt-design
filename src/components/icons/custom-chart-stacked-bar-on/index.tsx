import * as React from 'react';
import { ReactComponent as Icon } from './custom-chart-stacked-bar-on.svg';
import createIcon from '../create-icon';

const CustomChartStackedBarOn = createIcon(Icon);
export default React.memo(CustomChartStackedBarOn);
