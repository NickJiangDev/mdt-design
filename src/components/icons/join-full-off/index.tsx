import * as React from 'react';
import { ReactComponent as Icon } from './join-full-off.svg';
import createIcon from '../create-icon';

const JoinFullOff = createIcon(Icon);
export default React.memo(JoinFullOff);
