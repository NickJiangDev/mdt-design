import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-progress-circle-on.svg';
import createIcon from '../create-icon';

const LightChartProgressCircleOn = createIcon(Icon);
export default React.memo(LightChartProgressCircleOn);
