import * as React from 'react';
import { ReactComponent as Icon } from './axis-right.svg';
import createIcon from '../create-icon';

const AxisRight = createIcon(Icon);
export default React.memo(AxisRight);
