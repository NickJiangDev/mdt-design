import * as React from 'react';
import { ReactComponent as Icon } from './refresh-2.svg';
import createIcon from '../create-icon';

const Refresh2 = createIcon(Icon);
export default React.memo(Refresh2);
