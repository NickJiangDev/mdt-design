import * as React from 'react';
import { ReactComponent as Icon } from './format-align-justify.svg';
import createIcon from '../create-icon';

const FormatAlignJustify = createIcon(Icon);
export default React.memo(FormatAlignJustify);
