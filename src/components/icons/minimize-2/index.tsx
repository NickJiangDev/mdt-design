import * as React from 'react';
import { ReactComponent as Icon } from './minimize-2.svg';
import createIcon from '../create-icon';

const Minimize2 = createIcon(Icon);
export default React.memo(Minimize2);
