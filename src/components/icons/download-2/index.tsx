import * as React from 'react';
import { ReactComponent as Icon } from './download-2.svg';
import createIcon from '../create-icon';

const Download2 = createIcon(Icon);
export default React.memo(Download2);
