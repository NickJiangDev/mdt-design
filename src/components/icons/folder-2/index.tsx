import * as React from 'react';
import { ReactComponent as Icon } from './folder-2.svg';
import createIcon from '../create-icon';

const Folder2 = createIcon(Icon);
export default React.memo(Folder2);
