import * as React from 'react';
import { ReactComponent as Icon } from './custom-chart-percent-bar-on.svg';
import createIcon from '../create-icon';

const CustomChartPercentBarOn = createIcon(Icon);
export default React.memo(CustomChartPercentBarOn);
