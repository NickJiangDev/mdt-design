import * as React from 'react';
import { ReactComponent as Icon } from './map-mark-2.svg';
import createIcon from '../create-icon';

const MapMark2 = createIcon(Icon);
export default React.memo(MapMark2);
