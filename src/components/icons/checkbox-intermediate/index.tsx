import * as React from 'react';
import { ReactComponent as Icon } from './checkbox-intermediate.svg';
import createIcon from '../create-icon';

const CheckboxIntermediate = createIcon(Icon);
export default React.memo(CheckboxIntermediate);
