import * as React from 'react';
import { ReactComponent as Icon } from './light-project-lab-off.svg';
import createIcon from '../create-icon';

const LightProjectLabOff = createIcon(Icon);
export default React.memo(LightProjectLabOff);
