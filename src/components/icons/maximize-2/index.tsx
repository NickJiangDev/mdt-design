import * as React from 'react';
import { ReactComponent as Icon } from './maximize-2.svg';
import createIcon from '../create-icon';

const Maximize2 = createIcon(Icon);
export default React.memo(Maximize2);
