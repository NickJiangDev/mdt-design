import * as React from 'react';
import { ReactComponent as Icon } from './launch-2.svg';
import createIcon from '../create-icon';

const Launch2 = createIcon(Icon);
export default React.memo(Launch2);
