import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-mixed-bar-on.svg';
import createIcon from '../create-icon';

const LightChartMixedBarOn = createIcon(Icon);
export default React.memo(LightChartMixedBarOn);
