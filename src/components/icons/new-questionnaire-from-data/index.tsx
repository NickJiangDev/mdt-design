import * as React from 'react';
import { ReactComponent as Icon } from './new-questionnaire-from-data.svg';
import createIcon from '../create-icon';

const NewQuestionnaireFromData = createIcon(Icon);
export default React.memo(NewQuestionnaireFromData);
