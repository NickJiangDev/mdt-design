import * as React from 'react';
import { ReactComponent as Icon } from './format-align-top.svg';
import createIcon from '../create-icon';

const FormatAlignTop = createIcon(Icon);
export default React.memo(FormatAlignTop);
