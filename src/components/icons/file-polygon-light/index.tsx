import * as React from 'react';
import { ReactComponent as Icon } from './file-polygon-light.svg';
import createIcon from '../create-icon';

const FilePolygonLight = createIcon(Icon);
export default React.memo(FilePolygonLight);
