import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-sunburst-on.svg';
import createIcon from '../create-icon';

const LightChartSunburstOn = createIcon(Icon);
export default React.memo(LightChartSunburstOn);
