import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-stacked-bar-on.svg';
import createIcon from '../create-icon';

const LightChartStackedBarOn = createIcon(Icon);
export default React.memo(LightChartStackedBarOn);
