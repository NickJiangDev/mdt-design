import * as React from 'react';
import { ReactComponent as Icon } from './maximize-3.svg';
import createIcon from '../create-icon';

const Maximize3 = createIcon(Icon);
export default React.memo(Maximize3);
