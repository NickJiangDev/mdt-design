import * as React from 'react';
import { ReactComponent as Icon } from './light-project-screen-on.svg';
import createIcon from '../create-icon';

const LightProjectScreenOn = createIcon(Icon);
export default React.memo(LightProjectScreenOn);
