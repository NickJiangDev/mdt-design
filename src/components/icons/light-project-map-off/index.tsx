import * as React from 'react';
import { ReactComponent as Icon } from './light-project-map-off.svg';
import createIcon from '../create-icon';

const LightProjectMapOff = createIcon(Icon);
export default React.memo(LightProjectMapOff);
