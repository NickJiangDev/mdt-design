import * as React from 'react';
import { ReactComponent as Icon } from './light-project-mobile-off.svg';
import createIcon from '../create-icon';

const LightProjectMobileOff = createIcon(Icon);
export default React.memo(LightProjectMobileOff);
