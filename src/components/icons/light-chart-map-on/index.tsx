import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-map-on.svg';
import createIcon from '../create-icon';

const LightChartMapOn = createIcon(Icon);
export default React.memo(LightChartMapOn);
