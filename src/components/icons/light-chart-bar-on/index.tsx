import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-bar-on.svg';
import createIcon from '../create-icon';

const LightChartBarOn = createIcon(Icon);
export default React.memo(LightChartBarOn);
