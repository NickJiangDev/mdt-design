import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-rose-pie-on.svg';
import createIcon from '../create-icon';

const LightChartRosePieOn = createIcon(Icon);
export default React.memo(LightChartRosePieOn);
