import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-radar-off.svg';
import createIcon from '../create-icon';

const LightChartRadarOff = createIcon(Icon);
export default React.memo(LightChartRadarOff);
