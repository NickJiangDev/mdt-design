import * as React from 'react';
import { ReactComponent as Icon } from './join-full-on.svg';
import createIcon from '../create-icon';

const JoinFullOn = createIcon(Icon);
export default React.memo(JoinFullOn);
