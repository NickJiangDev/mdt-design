import * as React from 'react';
import { ReactComponent as Icon } from './minimize-3.svg';
import createIcon from '../create-icon';

const Minimize3 = createIcon(Icon);
export default React.memo(Minimize3);
