import * as React from 'react';
import { ReactComponent as Icon } from './done-check-2.svg';
import createIcon from '../create-icon';

const DoneCheck2 = createIcon(Icon);
export default React.memo(DoneCheck2);
