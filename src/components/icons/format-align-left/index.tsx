import * as React from 'react';
import { ReactComponent as Icon } from './format-align-left.svg';
import createIcon from '../create-icon';

const FormatAlignLeft = createIcon(Icon);
export default React.memo(FormatAlignLeft);
