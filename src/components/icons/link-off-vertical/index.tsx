import * as React from 'react';
import { ReactComponent as Icon } from './link-off-vertical.svg';
import createIcon from '../create-icon';

const LinkOffVertical = createIcon(Icon);
export default React.memo(LinkOffVertical);
