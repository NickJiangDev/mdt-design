import * as React from 'react';
import { ReactComponent as Icon } from './radio-on.svg';
import createIcon from '../create-icon';

const RadioOn = createIcon(Icon);
export default React.memo(RadioOn);
