import * as React from 'react';
import { ReactComponent as Icon } from './distribute-ver-spacing.svg';
import createIcon from '../create-icon';

const DistributeVerSpacing = createIcon(Icon);
export default React.memo(DistributeVerSpacing);
