import * as React from 'react';
import { ReactComponent as Icon } from './vertical-align-center.svg';
import createIcon from '../create-icon';

const VerticalAlignCenter = createIcon(Icon);
export default React.memo(VerticalAlignCenter);
