import * as React from 'react';
import { ReactComponent as Icon } from './light-project-resource-on.svg';
import createIcon from '../create-icon';

const LightProjectResourceOn = createIcon(Icon);
export default React.memo(LightProjectResourceOn);
