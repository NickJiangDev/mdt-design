import * as React from 'react';
import { ReactComponent as Icon } from './new-upload-link-light.svg';
import createIcon from '../create-icon';

const NewUploadLinkLight = createIcon(Icon);
export default React.memo(NewUploadLinkLight);
