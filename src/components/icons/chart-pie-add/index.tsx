import * as React from 'react';
import { ReactComponent as Icon } from './chart-pie-add.svg';
import createIcon from '../create-icon';

const ChartPieAdd = createIcon(Icon);
export default React.memo(ChartPieAdd);
