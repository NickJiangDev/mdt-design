import * as React from 'react';
import { ReactComponent as Icon } from './format-align-ver-center.svg';
import createIcon from '../create-icon';

const FormatAlignVerCenter = createIcon(Icon);
export default React.memo(FormatAlignVerCenter);
