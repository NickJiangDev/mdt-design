import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-pie-on.svg';
import createIcon from '../create-icon';

const LightChartPieOn = createIcon(Icon);
export default React.memo(LightChartPieOn);
