import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-word-index-on.svg';
import createIcon from '../create-icon';

const LightChartWordIndexOn = createIcon(Icon);
export default React.memo(LightChartWordIndexOn);
