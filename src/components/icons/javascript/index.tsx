import * as React from 'react';
import { ReactComponent as Icon } from './javascript.svg';
import createIcon from '../create-icon';

const Javascript = createIcon(Icon);
export default React.memo(Javascript);
