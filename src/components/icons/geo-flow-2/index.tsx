import * as React from 'react';
import { ReactComponent as Icon } from './geo-flow-2.svg';
import createIcon from '../create-icon';

const GeoFlow2 = createIcon(Icon);
export default React.memo(GeoFlow2);
