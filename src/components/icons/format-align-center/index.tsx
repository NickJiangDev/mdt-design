import * as React from 'react';
import { ReactComponent as Icon } from './format-align-center.svg';
import createIcon from '../create-icon';

const FormatAlignCenter = createIcon(Icon);
export default React.memo(FormatAlignCenter);
