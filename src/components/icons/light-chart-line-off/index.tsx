import * as React from 'react';
import { ReactComponent as Icon } from './light-chart-line-off.svg';
import createIcon from '../create-icon';

const LightChartLineOff = createIcon(Icon);
export default React.memo(LightChartLineOff);
